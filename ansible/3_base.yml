---
- hosts: fileserver2.grote.lan
  max_fail_percentage: 20%
  roles:
    - { role: mgrote.set_apt_sources, tags: "apt_sources" }
    - { role: mgrote.create_users, tags: "users" }
    - { role: mgrote.set_timezone, tags: "timezone" }
    - { role: mgrote.motd, tags: "motd" }
    - { role: mgrote.restic, tags: "restic" }
    - { role: igor_mukhin.bash_aliases, tags: "aliases", become: true }

  tasks:
    - name: Loesche Nutzer "neu"
      become: true
      user:
        name: neu
        state: absent
        force: yes
        remove: yes

  vars:
      base_users:
        - { name: 'mg', password: 'hallowelt', groups: 'sudo, ssh'}
