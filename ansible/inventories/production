all:
  children:
    hypervisor:
      hosts:
        pve1.ausb.md
        pve2.ausb.md
    dns:
      hosts:
        dns1.ausb.md:
    storage:
      hosts:
        fileserver1.ausb.md:
    acng:
      hosts:
        acng.ausb.md:
    ansible:
      hosts:
        ansible.ausb.md:
    docker:
      hosts:
        docker.ausb.md:
    git:
      hosts:
        git.ausb.md:
    jumphost:
      hosts:
        jump.ausb.md:
    ansible:
      hosts:
        ansible.ausb.md:
